&nbsp;
{{#with (changeset this.model this.RobotValidation) as |changeset|}}
  <RobotDetail
    @competitions={{this.competitions}}
    @changeset={{changeset}}
    @done="done"
    @updateCompetition="updateCompetition"
    @model={{this.model}}
  />
{{/with}}
